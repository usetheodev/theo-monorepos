version: 1
project: "{{project-name}}"

apps:
  api:
    path: apps/api
    framework: spring-boot
    type: server
    port: 8080

  worker:
    path: apps/worker
    framework: spring-boot
    type: worker
