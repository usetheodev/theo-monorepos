# {{project-name}}

Java API server built with [Spring Boot](https://spring.io/projects/spring-boot), scaffolded by [Theo](https://usetheo.dev).

## Development

```bash
./gradlew bootRun
```

The server starts on `http://localhost:8080`.

## Endpoints

- `GET /` — Hello message
- `GET /health` — Health check

## Deploy

```bash
theo deploy
```
