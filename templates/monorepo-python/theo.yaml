version: 1
project: "{{project-name}}"

apps:
  api:
    path: apps/api
    framework: fastapi
    type: server
    port: 8000

  worker:
    path: apps/worker
    framework: custom
    type: worker
    port: 8001
